[Unit]
Description=Restash failure notification (%i)

[Service]
Type=oneshot
ExecCondition=/bin/sh -c 'test "$(systemctl --user show -p ActiveState --value %i)" = failed'
ExecStart=notify-send -u critical "Restash failed" "%i\nSee: journalctl --user -u %i"
